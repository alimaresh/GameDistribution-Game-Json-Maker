# GameDistribution-Game-Json-Maker
A desktop GUI tool that parses GameDistribution HTML page source and extracts structured JSON including title, description, tags, size, URL, and thumbnail. Just paste the page source, extract, preview the image, and copy or save the JSON for use in custom game portals or API systems.
